spring:
  application:
    name: Configuration

  h2:
   console:
     enabled: true
     path: /h2-console #주의: 웹 브라우저에서 꼭 http://localhost:8080/h2-console 이 URL로 접속해야 함!!!

logging:
   level:
     root: INFO
     com:
       intheeast:
         demo: INFO
     org:
       hibernate:
         SQL: DEBUG
         orm:
           jdbc:
             bind: TRACE
  
      
  